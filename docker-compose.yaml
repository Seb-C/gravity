version: '3.8'

services:
  watch:
    image: node:17-alpine
    volumes:
      - $PWD:/gravity
    working_dir: /gravity
    command: npx tsc --watch --preserveWatchOutput
  http:
    image: nginx:alpine
    volumes:
      - $PWD:/usr/share/nginx/html
    ports:
      - "80:80"
